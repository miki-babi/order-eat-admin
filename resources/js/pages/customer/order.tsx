import CustomerMenuPage, { type CustomerMenuPageProps } from '@/components/customer/order/order-page';

export default function MenuPage(props: CustomerMenuPageProps) {
    return <CustomerMenuPage {...props} />;
}
